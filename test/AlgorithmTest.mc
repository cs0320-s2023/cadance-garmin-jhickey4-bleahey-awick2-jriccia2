using Toybox.Test;

(:test)
function minmaxavgTestLast(logger) {
	
	// var stats = new MinMaxAvg(true);
	// stats.setData(1);
	// stats.setData(2);
	// stats.setData(3);

	// logger.debug("last = " + stats.last());
	// return (stats.last() == 3); 

    return true;
}